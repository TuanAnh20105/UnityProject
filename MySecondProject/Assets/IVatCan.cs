using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IVatCan 
{
    // Start is called before the first frame update
     void SetEvent();
     void SetInvoke();
}
